{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Debug Operator (Local)",
        "type": "go",
        "request": "launch",
        "mode": "auto",
        "program": "${workspaceFolder}/main.go",
        "env": {
          "KUBECONFIG": "${env:HOME}/.kube/config"
        },
        "args": [],
        "cwd": "${workspaceFolder}"
      }
    ]
  }
  