---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: agents.core.inference-gateway.com
spec:
  group: core.inference-gateway.com
  names:
    kind: Agent
    listKind: AgentList
    plural: agents
    singular: agent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Version of the Agent resource
      jsonPath: .status.card.version
      name: VERSION
      type: string
    - description: URL of the Agent resource
      jsonPath: .status.card.url
      name: URL
      type: string
    - description: Streaming Capability of the Agent resource
      jsonPath: .status.card.capabilities.streaming
      name: STREAMING
      type: string
    - description: Push Notifications Capability of the Agent resource
      jsonPath: .status.card.capabilities.pushNotifications
      name: PUSH NOTIFICATIONS
      type: string
    - description: State Transition History Capability of the Agent resource
      jsonPath: .status.card.capabilities.stateTransitionHistory
      name: STATE TRANSITION HISTORY
      type: string
    - description: Age of the resource
      jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Agent is the Schema for the agents API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentSpec defines the desired state of Agent.
            properties:
              agent:
                description: Agent-specific configuration (LLM, retries, etc.).
                properties:
                  apiKey:
                    description: |-
                      APIKey holds a reference to the secret containing the LLM API key.
                      Deprecated: use spec.agent.llm.apiKeySecretRef instead.
                    properties:
                      secretRef:
                        type: string
                    type: object
                  enabled:
                    description: Enabled toggles the agent loop.
                    type: boolean
                  llm:
                    description: LLM configuration for the agent's language model.
                    properties:
                      apiKeySecretRef:
                        description: |-
                          APIKeySecretRef references a Secret key that contains the LLM API key.
                          Emitted as A2A_AGENT_CLIENT_API_KEY via valueFrom.secretKeyRef.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      baseURL:
                        description: |-
                          BaseURL is the base URL for the LLM API endpoint (e.g. the Inference Gateway URL).
                          Emitted as A2A_AGENT_CLIENT_BASE_URL.
                        type: string
                      customHeaders:
                        description: CustomHeaders are additional HTTP headers sent
                          with each LLM request.
                        items:
                          properties:
                            name:
                              type: string
                            value:
                              type: string
                          required:
                          - name
                          - value
                          type: object
                        type: array
                      maxTokens:
                        default: 4096
                        description: |-
                          MaxTokens is the maximum number of tokens to generate.
                          Emitted as A2A_AGENT_CLIENT_MAX_TOKENS.
                        format: int32
                        type: integer
                      model:
                        description: |-
                          Model is the model identifier in "provider/model" format.
                          The provider prefix is split out and emitted as A2A_AGENT_CLIENT_PROVIDER;
                          the remainder is emitted as A2A_AGENT_CLIENT_MODEL.
                        type: string
                      systemPrompt:
                        description: SystemPrompt is the system prompt for the LLM.
                        type: string
                      temperature:
                        default: "0.7"
                        description: |-
                          Temperature controls the randomness of the LLM output.
                          Emitted as A2A_AGENT_CLIENT_TEMPERATURE.
                        type: string
                    type: object
                  maxChatCompletionIterations:
                    default: 5
                    description: MaxChatCompletionIterations is the maximum number
                      of LLM call iterations per request.
                    format: int32
                    type: integer
                  maxConversationHistory:
                    default: 10
                    description: MaxConversationHistory is the number of messages
                      retained in context.
                    format: int32
                    type: integer
                  maxRetries:
                    default: 3
                    description: MaxRetries is the maximum number of retries for failed
                      LLM calls.
                    format: int32
                    type: integer
                  tls:
                    description: TLS configuration for agent-to-agent communication.
                    properties:
                      enabled:
                        description: Enabled toggles TLS.
                        type: boolean
                      secretRef:
                        description: |-
                          SecretRef is the name of the Secret holding TLS credentials.
                          Only required when enabled is true.
                        type: string
                    type: object
                type: object
              env:
                description: Environment variables for the agent container.
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              host:
                default: 0.0.0.0
                description: Host address the agent binds to.
                type: string
              idleTimeout:
                default: 60s
                description: IdleTimeout for HTTP server.
                type: string
              image:
                description: Image is the container image for the agent.
                type: string
              logging:
                description: Logging configuration.
                properties:
                  format:
                    default: json
                    description: Format sets the log output format.
                    type: string
                  level:
                    default: info
                    description: Level sets the log verbosity.
                    type: string
                type: object
              port:
                default: 8080
                description: Port the agent listens on.
                format: int32
                type: integer
              queue:
                description: Queue configuration.
                properties:
                  cleanupInterval:
                    description: |-
                      CleanupInterval is how often completed items are purged from the queue.
                      Only meaningful when enabled is true.
                    type: string
                  enabled:
                    description: Enabled toggles the task queue.
                    type: boolean
                  maxSize:
                    description: |-
                      MaxSize is the maximum number of items in the queue.
                      Only meaningful when enabled is true.
                    format: int32
                    type: integer
                type: object
              readTimeout:
                default: 30s
                description: ReadTimeout for HTTP server.
                type: string
              telemetry:
                description: Telemetry configuration.
                properties:
                  enabled:
                    default: false
                    description: Enable telemetry collection
                    type: boolean
                  metrics:
                    default:
                      enabled: false
                      port: 9464
                    description: Metrics configuration
                    properties:
                      enabled:
                        default: false
                        description: Enable metrics collection
                        type: boolean
                      port:
                        default: 9464
                        description: Port for metrics endpoint
                        format: int32
                        maximum: 65535
                        minimum: 1024
                        type: integer
                    type: object
                type: object
              timezone:
                default: UTC
                description: Timezone for the agent process.
                type: string
              tls:
                description: TLS configuration for the agent HTTP server.
                properties:
                  enabled:
                    description: Enabled toggles TLS.
                    type: boolean
                  secretRef:
                    description: |-
                      SecretRef is the name of the Secret holding TLS credentials.
                      Only required when enabled is true.
                    type: string
                type: object
              writeTimeout:
                default: 30s
                description: WriteTimeout for HTTP server.
                type: string
            required:
            - image
            type: object
          status:
            description: AgentStatus defines the observed state of Agent.
            properties:
              card:
                description: Card indicates the version of the Agent resource.
                properties:
                  capabilities:
                    properties:
                      pushNotifications:
                        type: boolean
                      stateTransitionHistory:
                        type: boolean
                      streaming:
                        type: boolean
                    required:
                    - pushNotifications
                    - stateTransitionHistory
                    - streaming
                    type: object
                  defaultInputModes:
                    items:
                      type: string
                    type: array
                  defaultOutputModes:
                    items:
                      type: string
                    type: array
                  description:
                    type: string
                  documentationUrl:
                    description: DocumentationURL is an optional field that provides
                      a URL to the documentation for the Agent.
                    type: string
                  name:
                    type: string
                  skills:
                    items:
                      properties:
                        description:
                          type: string
                        examples:
                          items:
                            type: string
                          type: array
                        id:
                          type: string
                        inputModes:
                          items:
                            type: string
                          type: array
                        name:
                          type: string
                        outputModes:
                          items:
                            type: string
                          type: array
                        tags:
                          items:
                            type: string
                          type: array
                      required:
                      - description
                      - examples
                      - id
                      - inputModes
                      - name
                      - outputModes
                      - tags
                      type: object
                    type: array
                  skillsNames:
                    description: Comma separated string of skill names.
                    type: string
                  url:
                    type: string
                  version:
                    type: string
                required:
                - capabilities
                - defaultInputModes
                - defaultOutputModes
                - description
                - name
                - skills
                - url
                - version
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                enum:
                - Pending
                - Running
                - Failed
                - Unknown
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this resource.
                format: int64
                type: integer
              ready:
                description: Ready indicates if the resource is ready.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
